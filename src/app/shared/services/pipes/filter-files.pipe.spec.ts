import { FilterFilesPipe } from './filter-files.pipe';

describe('FilterFilesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFilesPipe();
    expect(pipe).toBeTruthy();
  });
});
