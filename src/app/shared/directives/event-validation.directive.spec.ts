import { EventValidationDirective } from './event-validation.directive';

describe('EventValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new EventValidationDirective();
    expect(directive).toBeTruthy();
  });
});
